function [] = Print_Stage_Infos(Obj)
  % tic;
  % Obj.VPrintF_With_ID('Homing stage...');
  % Obj.Dev.home();
  % Obj.Wait_Ready();
  % Obj.Done();
  % STEP_SIZE; % [mm] one microstep = 0.2 micron
  % RANGE; % [mm] min / max travel range
  % MAX_SPEED; % [mm/s] max speed limit = maxspeed setting of 12288000
  % DEFAULT_VEL;
end


